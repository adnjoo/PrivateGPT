# TGBot

### Instructions
```bash
python -m venv venv
source venv/bin/activate # mac. for Windows, use venv/Scripts/activate

pip install -r requirements.txt
python main.py
```

Put in .env
```bash
BOT_TOKEN=...
```
